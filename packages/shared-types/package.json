{
  "name": "@auth-system/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types for authentication system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "mkdir -p dist && echo 'Build types placeholder'",
    "dev": "echo 'Dev types placeholder'",
    "lint": "echo 'Lint types placeholder'",
    "type-check": "echo 'Type-check types placeholder'",
    "clean": "rm -rf dist .turbo && echo 'Clean types placeholder'"
  },
  "devDependencies": {
    "@auth-system/eslint-config": "workspace:*"
  }
}
