{
  "name": "@auth-system/backend",
  "version": "0.1.0",
  "private": true,
  "description": "NestJS backend with hexagonal architecture for authentication system",
  "scripts": {
    "build": "mkdir -p dist && echo 'Backend build placeholder'",
    "dev": "echo 'Backend dev placeholder'",
    "start": "echo 'Backend start placeholder'",
    "test": "mkdir -p coverage && echo 'Backend test placeholder'",
    "test:watch": "echo 'Backend test:watch placeholder'",
    "lint": "echo 'Backend lint placeholder'",
    "type-check": "echo 'Backend type-check placeholder'",
    "clean": "rm -rf coverage dist .turbo && echo 'Backend clean placeholder'"
  },
  "devDependencies": {
    "@auth-system/eslint-config": "workspace:*",
    "@auth-system/types": "workspace:*"
  }
}
