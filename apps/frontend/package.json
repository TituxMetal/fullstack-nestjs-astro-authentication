{
  "name": "@auth-system/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Astro frontend with React integration for authentication system",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && echo 'Frontend build placeholder - will be updated in Issue #9'",
    "dev": "mkdir -p .astro && echo 'Frontend dev placeholder - will be updated in Issue #9' && tsc --watch -p tsconfig.json",
    "start": "echo 'Frontend start placeholder - will be updated in Issue #9'",
    "test": "mkdir -p coverage && echo 'Frontend test placeholder - will be updated in Issue #9'",
    "test:watch": "echo 'Frontend test:watch placeholder - will be updated in Issue #9'",
    "lint": "eslint . --fix",
    "lint:check": "eslint .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf coverage dist .astro .turbo"
  },
  "devDependencies": {
    "@auth-system/eslint-config": "workspace:*",
    "@auth-system/ts-config": "workspace:*",
    "@auth-system/types": "workspace:*"
  }
}
