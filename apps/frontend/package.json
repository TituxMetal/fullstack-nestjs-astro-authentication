{
  "name": "@auth-system/frontend",
  "version": "0.1.0",
  "private": true,
  "description": "Astro frontend with React integration for authentication system",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && echo 'Frontend build placeholder'",
    "dev": "mkdir -p .astro && echo 'Frontend dev placeholder'",
    "start": "echo 'Frontend start placeholder'",
    "test": "mkdir -p coverage && echo 'Frontend test placeholder'",
    "test:watch": "echo 'Frontend test:watch placeholder'",
    "lint": "echo 'Frontend lint placeholder'",
    "type-check": "echo 'Frontend type-check placeholder'",
    "clean": "rm -rf coverage dist .astro .turbo && echo 'Frontend clean placeholder'"
  },
  "devDependencies": {
    "@auth-system/eslint-config": "workspace:*",
    "@auth-system/types": "workspace:*"
  }
}
